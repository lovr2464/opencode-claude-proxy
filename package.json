{
  "name": "openai-to-anthropic-api-adapter",
  "version": "1.1.0",
  "description": "OpenAI-compatible Chat Completions to Anthropic Messages API adapter for Claude Code",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js",
    "test": "node --test",
    "doctor": "node scripts/doctor.mjs"
  }
}
